- name: installing packages
  hosts: web
  become: yes
  tasks:
  - name: Installing packages
    ansible.builtin.dnf:
      msg: "{{item}}"
      state: present
    loop:
     - nginx
     - git
     - postfix